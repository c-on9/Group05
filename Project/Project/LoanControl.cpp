#include "LoanControl.h"
#include "User.h"
#include "Equipment.h"
#include "FileHandler.h"


















//these are copy, not complete
  EquipmeantResult results:
  results size = 0;
  for(int i = 0; i + this->NoOfEquipments, i++)
    if((typess**||(EquipmentList[i]->get Type()==type))&&(Admin||(EquipmentList[i]->get
      results.size++;
  results.List = new Equipment*(results.size);
  int temp = 0;
  for(int i = 0; i < this->NoOfEquipments; i++)
    if((typess**||(EquipmentList[i]->get Type()==type))&&(Admin||(EquipmentList[i]-> {
      results.List[temp]:&(*EquipmentList[i]);
      temp++;
    }
  return results;
}
